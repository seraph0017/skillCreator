I will add a new skill for Feishu Tasks (`feishu-tasks`) to complement the existing `feishu-calendar` skill. This will follow the same high-quality structure as the calendar skill, including a helper client and documentation.

### Plan

1.  **Create Skill Directory**:
    -   Create `.trae/skills/feishu-tasks/`.

2.  **Implement Task Client (`feishu_task_client.py`)**:
    -   Create a Python client in the skill directory to handle Feishu Task V2 API.
    -   **Auth**: Reuse the logic to manage `tenant_access_token`.
    -   **Config**: Support interactive initialization (App ID, Secret) and save to `~/.feishu_task_config.json`.
    -   **Features**:
        -   `create_task`: Support summary, due date, origin, etc.
        -   `list_tasks`: Get tasks (with pagination support).
        -   `complete_task`: Mark task as completed.
        -   `delete_task`: Delete a task.

3.  **Create Skill Documentation (`SKILL.md`)**:
    -   Define the skill metadata (name: `feishu-tasks`).
    -   Document the API capabilities and how to use the `feishu_task_client.py`.
    -   Provide code examples for the model to use.

4.  **Update Project README**:
    -   Add `feishu-tasks` to the list of available skills in the root `README.md`.

5.  **Verification**:
    -   I will verify the file creation and basic syntax.
